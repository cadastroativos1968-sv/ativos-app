services:
  mysql:
    image: mysql:8.0
    platform: linux/amd64
    container_name: mysql-local
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: root123
      MYSQL_DATABASE: glpisv_glpi
      MYSQL_USER: cadastro_ativo
      MYSQL_PASSWORD: JtAm82jshv
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data:
